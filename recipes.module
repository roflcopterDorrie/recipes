<?php

use Drupal\node\Entity\Node;

/**
 * Implements hook_theme().
 */
function recipes_theme($existing, $type, $theme, $path) {
  return [
    'recipe_preview' => [
      'variables' => [
        'recipe' => NULL,
      ],
      'template' => 'recipe-preview',
    ],
  ];
}
