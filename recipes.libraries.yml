# This adds the plugin JavaScript to the page.
# cSpell:ignore simplebox demobox

insertIngredient:
  js:
    js/build/insertIngredient.js: { preprocess: false, minified: true }
  css:
    theme:
      css/insert_ingredient.admin.css: {} 
  dependencies:
    - core/ckeditor5

ingredient_search:
  js:
    js/ingredient_search/ingredient_search.js: { attributes: { type: module } }
  dependencies:
    - core/drupal
    - core/once