langcode: en
status: true
dependencies:
  config:
    - user.role.recipes_user
  module:
    - user
  enforced:
    module:
      - recipes
id: user_add_role_action.recipes_user
label: 'Add the Recipe user role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: recipes_user
